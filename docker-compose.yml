services:
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production

  app-1:
    build:
      context: .
      dockerfile: apps/app-1/Dockerfile
    ports:
      - "8080:80"
